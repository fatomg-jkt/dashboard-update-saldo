import { categories, type Brand, type FiltersState } from '../types';
export function Filters({ filters, brands, onChange, onReset }: { filters: FiltersState; brands: Brand[]; onChange: (f: FiltersState) => void; onReset: () => void; }) { return <div className="grid gap-2 rounded-xl border bg-white p-3 shadow-sm md:grid-cols-4"><input className="input" placeholder="Cari brand, bank, no rek, metode..." value={filters.search} onChange={e=>onChange({...filters,search:e.target.value})}/><select className="input" value={filters.category} onChange={e=>onChange({...filters,category:e.target.value as FiltersState['category']})}><option>Semua</option>{categories.map(c=><option key={c}>{c}</option>)}</select><select className="input" value={filters.brandId} onChange={e=>onChange({...filters,brandId:e.target.value})}><option>Semua</option>{brands.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><button className="btn" onClick={onReset}>Reset Filter</button></div> }
