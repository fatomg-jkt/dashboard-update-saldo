import type { BalanceState } from '../types';
export function ImportExportPanel({ data, onImport, onReset }: { data: BalanceState; onImport: (data: BalanceState) => void; onReset: () => void; }) { const exportJson=()=>{const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='dashboard-update-saldo.json'; a.click(); URL.revokeObjectURL(a.href)}; return <div className="flex flex-wrap gap-2"><button className="btn" onClick={onReset}>Reset ke Sample Data</button><button className="btn" onClick={exportJson}>Export JSON</button><label className="btn cursor-pointer">Import JSON<input type="file" accept="application/json" className="hidden" onChange={async e=>{const file=e.target.files?.[0]; if(!file) return; try{onImport(JSON.parse(await file.text()) as BalanceState)}catch{alert('File JSON tidak valid')}}}/></label></div> }
